<!DOCTYPE html>
<html>
    <head>
        <title>Update my profile</title>
        <% include partial/header.ejs %>
    </head>
    <body>
        <% include partial/navbar.ejs %>
        <h1>Editing your car <%= row.plate_num %></h1>
        <% include partial/msg.ejs %>
        <form action="/editcar?plate_num=<%= encodeURI(row.plate_num) %>" role="form" method="post">
            <div class="form-group">
                <label>Model (in 7 chars) <input type="text" class="form-control" name="model" value="<%= row.model %>"></label>
            </div>
            <div class="form-group">
                <label>How many passenger seats?
                    <input type="number" min="1" max="100" class="form-control" name="num_seats" value="<%= row.num_seats %>"></label>
            </div>
            <div class="form-group">
                <label>COE Expiry (1 year of remaining validity needed)
                    <input type="date" class="form-control" name="edate" value="<%= row.edate.toISOString().split('T')[0] %>"></label>
            </div>
            <input type="submit" class="btn btn-primary" value="Edit car">
        </form>
    </body>
</html>
